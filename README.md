# Ansible role to configure Strongswan as a client
This role provides a means to configure Strongswan as a IPSEC client on Debian systems.

Check [defaults/main.yml](the defaults file) for an example setup.
